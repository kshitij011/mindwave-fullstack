interface Embeddings {
    embedTexts(texts: string[]): Promise<number[][]>;
}
declare class RemoteModelEmbeddings implements Embeddings {
    private model;
    private apiKey;
    private baseUrl;
    private port?;
    constructor(model: string, apiKey: string, baseUrl: string, port?: number | string);
    embedTexts(texts: string[]): Promise<number[][]>;
}
export { type Embeddings, RemoteModelEmbeddings };
