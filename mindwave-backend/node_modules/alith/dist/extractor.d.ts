import { z } from "zod";
import { Agent } from "./agent";
export declare class Extractor {
    agent: Agent;
    model: z.ZodSchema;
    constructor(agent: Agent, model: z.ZodSchema);
    extract(input: string): Promise<z.infer<typeof this.model>>;
}
export declare function parseArgs<TActionSchema extends z.ZodTypeAny = z.ZodTypeAny>(argsSchema: TActionSchema, ...args: any[]): z.infer<TActionSchema>;
